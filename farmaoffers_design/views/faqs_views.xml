<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- List -->
    <record id="view_fo_faqs_list" model="ir.ui.view">
        <field name="name">fo.faqs.list</field>
        <field name="model">fo.faqs</field>
        <field name="arch" type="xml">
            <list>
                <field name="title"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <!-- Form -->
    <record id="view_fo_faqs_form" model="ir.ui.view">
        <field name="name">fo.faqs.form</field>
        <field name="model">fo.faqs</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="title"/>
                        <field name="description"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_fo_faqs" model="ir.actions.act_window">
        <field name="name">FAQs</field>
        <field name="res_model">fo.faqs</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
